/*
 * ProductsList Messages
 *
 * This contains all the text for the ProductsList container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ProductsList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProductsList container!',
  },
});
