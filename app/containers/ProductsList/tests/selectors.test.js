// import { selectProductsListDomain } from '../selectors';

describe('selectProductsListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
